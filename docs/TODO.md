# TODO

## Content and code quality

- Run a repo-wide code similarity search, including source and plugin prompt
  content. Use it to identify duplicated helper logic, repeated agent/skill
  boilerplate, and "agent slop" prose/code that should be deleted, simplified,
  merged, or moved behind shared helper classes/modules.

