# Project-scoped Codex configuration (generated by maaaw-kit to-codex.py --write-config).
# Keep this file small and repo-specific. Do NOT set broad permissions, secrets,
# machine-specific paths, or aggressive approval policies here.

# Keep project docs lean (AGENTS.md + nested docs budget).
project_doc_max_bytes = 32768

# Optional per-skill toggles:
# [[skills.config]]
# path = ".agents/skills/codebase-audit/SKILL.md"
# enabled = true
