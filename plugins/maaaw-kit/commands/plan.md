---
description: Produce a structured implementation plan before coding (recon, approaches, load-bearing decision, verifiable steps)
argument-hint: <task description>
---
Read the deep-thinking skill and apply its full planning sequence to: $ARGUMENTS

Do real reconnaissance in the codebase before proposing anything. Output the plan in the skill's format (GOAL / RECON / APPROACH / LOAD-BEARING / STEPS / RISKS). Wait for my confirmation before implementing IF the load-bearing decision is uncertain or data is at risk — otherwise proceed step by step, verifying each step.
